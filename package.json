{
    "name": "full",
    "version": "1.0.0",
    "type": "module",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "start": "concurrently \"npm install && cd frontend && npm run build && npm start\" \"cd backend && npm start\"",
        "dev": "nodemon ./src/server.js"
    },
    "dependencies": {
        "backend": "file:",
        "concurrently": "^9.0.1",
        "full": "file:"
    },
    "devDependencies": {
        "concurrently": "^9.0.1"
    }
}
